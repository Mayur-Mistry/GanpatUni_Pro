@model IEnumerable<GanpatUni_Pro.Models.Announcement>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Desc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Document)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Announcement_Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mentors)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Group_Masters)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Desc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Document)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Announcement_Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mentors.Mentor_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Group_Masters.Group_Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Announcement_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Announcement_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Announcement_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
