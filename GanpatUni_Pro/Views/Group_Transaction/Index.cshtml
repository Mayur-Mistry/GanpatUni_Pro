@model IEnumerable<GanpatUni_Pro.Models.Group_Transaction>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Group_Masters)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Students)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Group_Masters.Group_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Students.Enroll_No)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GroupTransaction_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GroupTransaction_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GroupTransaction_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
